const assert = require('assert');
const myMath = require('../my-math.js');
describe('Array', function() {
	describe('#indexOf()', function() {
		it('should return 3 when doing 2+1', function() {
			assert.equal(3, myMath.add(2,1));
		});
	});
});
